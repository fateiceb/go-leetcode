package main

import "fmt"

func main() {
	s := "fleyctuuajsr"
	getLucky(s, 5)
}
func getLucky(s string, k int) int {
	nslic := make([]int, len(s))
	for j := 0; j < len(s); j++ {
		nslic[j] = int(s[j] - 96)
	}

	for i := 0;i<k ;i++{
		for 
	}
	return num
}
